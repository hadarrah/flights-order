{
  "name": "order-service",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build & run": "tsc -b && node dist/index.js",
    "build": "tsc -b",
    "run": "node dist/index.js"
  },
  "dependencies": {
    "@flights-order/mysql-client": "workspace:*",
    "@flights-order/redis-client": "workspace:*",
    "@flights-order/common-utils": "workspace:*",
    "express": "^4.19.2"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
